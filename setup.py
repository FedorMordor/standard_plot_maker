from setuptools import find_packages, setup

setup(
        name='standard_plot_maker',
        packages=find_packages(),
        version='0.0.1',
        description='Provides classes to make standard plots',
        author='FedorMordor',
        license='HeHe',
        )
